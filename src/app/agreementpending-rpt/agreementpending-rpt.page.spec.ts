import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgreementpendingRptPage } from './agreementpending-rpt.page';

describe('AgreementpendingRptPage', () => {
  let component: AgreementpendingRptPage;
  let fixture: ComponentFixture<AgreementpendingRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AgreementpendingRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
