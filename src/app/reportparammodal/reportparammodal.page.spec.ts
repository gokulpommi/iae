import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReportparammodalPage } from './reportparammodal.page';

describe('ReportparammodalPage', () => {
  let component: ReportparammodalPage;
  let fixture: ComponentFixture<ReportparammodalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReportparammodalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
