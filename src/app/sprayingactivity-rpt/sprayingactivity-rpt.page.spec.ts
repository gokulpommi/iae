import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SprayingactivityRptPage } from './sprayingactivity-rpt.page';

describe('SprayingactivityRptPage', () => {
  let component: SprayingactivityRptPage;
  let fixture: ComponentFixture<SprayingactivityRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SprayingactivityRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
