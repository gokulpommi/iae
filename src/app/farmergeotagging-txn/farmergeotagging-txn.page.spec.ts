import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FarmergeotaggingTxnPage } from './farmergeotagging-txn.page';

describe('FarmergeotaggingTxnPage', () => {
  let component: FarmergeotaggingTxnPage;
  let fixture: ComponentFixture<FarmergeotaggingTxnPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FarmergeotaggingTxnPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
