import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CroppurchaseRptPage } from './croppurchase-rpt.page';

describe('CroppurchaseRptPage', () => {
  let component: CroppurchaseRptPage;
  let fixture: ComponentFixture<CroppurchaseRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CroppurchaseRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
