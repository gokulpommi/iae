import { ComponentFixture, TestBed } from '@angular/core/testing';
import { StatuspendingupdateRptPage } from './statuspendingupdate-rpt.page';

describe('StatuspendingupdateRptPage', () => {
  let component: StatuspendingupdateRptPage;
  let fixture: ComponentFixture<StatuspendingupdateRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(StatuspendingupdateRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
