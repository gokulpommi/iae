import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GeoandareapendingRptPage } from './geoandareapending-rpt.page';

describe('GeoandareapendingRptPage', () => {
  let component: GeoandareapendingRptPage;
  let fixture: ComponentFixture<GeoandareapendingRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GeoandareapendingRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
