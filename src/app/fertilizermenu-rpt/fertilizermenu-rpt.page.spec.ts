import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FertilizermenuRptPage } from './fertilizermenu-rpt.page';

describe('FertilizermenuRptPage', () => {
  let component: FertilizermenuRptPage;
  let fixture: ComponentFixture<FertilizermenuRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FertilizermenuRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
