import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FertilizerupdateTxnPage } from './fertilizerupdate-txn.page';

describe('FertilizerupdateTxnPage', () => {
  let component: FertilizerupdateTxnPage;
  let fixture: ComponentFixture<FertilizerupdateTxnPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FertilizerupdateTxnPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
