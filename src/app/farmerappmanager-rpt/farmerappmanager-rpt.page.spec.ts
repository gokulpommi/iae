import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FarmerappmanagerRptPage } from './farmerappmanager-rpt.page';

describe('FarmerappmanagerRptPage', () => {
  let component: FarmerappmanagerRptPage;
  let fixture: ComponentFixture<FarmerappmanagerRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FarmerappmanagerRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
