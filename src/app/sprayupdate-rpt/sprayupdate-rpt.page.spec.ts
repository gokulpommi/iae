import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SprayupdateRptPage } from './sprayupdate-rpt.page';

describe('SprayupdateRptPage', () => {
  let component: SprayupdateRptPage;
  let fixture: ComponentFixture<SprayupdateRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SprayupdateRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
