import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FrmsummaryRptPage } from './frmsummary-rpt.page';

describe('FrmsummaryRptPage', () => {
  let component: FrmsummaryRptPage;
  let fixture: ComponentFixture<FrmsummaryRptPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FrmsummaryRptPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
