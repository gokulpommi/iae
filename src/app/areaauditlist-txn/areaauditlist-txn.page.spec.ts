import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AreaauditlistTxnPage } from './areaauditlist-txn.page';

describe('AreaauditlistTxnPage', () => {
  let component: AreaauditlistTxnPage;
  let fixture: ComponentFixture<AreaauditlistTxnPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AreaauditlistTxnPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
