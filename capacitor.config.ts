import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ninfo.farmpm',
  appName: 'India Agro',
  webDir: 'www',
  server : {
    cleartext:true
  },
};

export default config;
